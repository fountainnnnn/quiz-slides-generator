# --- Backend framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1

# --- Slides / PPTX export ---
python-pptx==1.0.2
pillow==10.4.0
tqdm==4.66.5

# --- PDF + OCR support ---
pymupdf==1.24.9
pypdfium2==4.30.0
easyocr==1.7.2
# Let EasyOCR install compatible torch + torchvision versions

# --- Array + image utils ---
numpy==1.26.4
imageio==2.37.0
opencv-contrib-python==4.10.0.84
opencv-python-headless==4.10.0.84

# --- LLM client ---
openai==1.107.1

# --- Google auth libs (only if you still need them for other APIs) ---
google-api-core==2.25.1
google-auth==2.40.3
google-auth-httplib2==0.2.0
googleapis-common-protos==1.70.0

# --- Serialization ---
protobuf==4.25.3
